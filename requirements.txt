# Dependências do PyTorch (Instalação manual recomendada)
# --------------------------------------------------------
# Use o comando do site oficial para obter a versão com CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# Dependências da Aplicação
# ---------------------------------
# Instale com: pip install -r requirements.txt (após o PyTorch)
ultralytics>=8.0.0 # Para YOLOv8
scikit-learn>=1.3.0 # Para KFold
Pillow>=10.0.0
watchdog>=3.0.0
numpy>=1.24.0